[build-system]
requires      = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name        = "promcp"
version     = "0.4.0"
description = "Proactive MCP — decorators and linter for proMCP-compliant MCP servers"
readme      = "README.md"
license     = "Apache-2.0"
license-files = ["THIRD_PARTY_LICENSES/*", "NOTICE"]
authors     = [
    { name = "@alexlqi", email = "alexlqi@users.noreply.github.com" },
]
keywords    = [
    "mcp", "model-context-protocol", "ai-agents",
    "llm", "agentic", "tool-design", "promcp",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
requires-python = ">=3.10"

dependencies = [
    "typing_extensions>=4.12",
]

[project.optional-dependencies]
yaml = ["pyyaml>=6.0.1"]
mcp  = ["mcp>=1.9"]
# Transporte MCP de primera clase, aislado tras `promcp.transport`. Ver ADR-001.
# Piso en 3.4 (fixes SSRF/OAuth + floor de Starlette por CVE-2026-48710); techo
# antes de 4.0 para no comerse un major sin querer. El PIN EXACTO auditado vive
# en el lockfile, no aqui.
transport = ["fastmcp>=3.4,<4"]
dev  = [
    "pytest>=8.2",
    "pyyaml>=6.0.1",
]

[project.urls]
Homepage      = "https://github.com/alexlqi/tmcp"
Repository    = "https://github.com/alexlqi/tmcp"
Documentation = "https://github.com/alexlqi/tmcp/blob/main/SPEC.md"
"Bug Tracker" = "https://github.com/alexlqi/tmcp/issues"

[project.scripts]
promcp-lint = "promcp.linter.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["promcp*"]

[tool.setuptools.package-data]
promcp = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]
